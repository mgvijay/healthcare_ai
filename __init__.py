"""
Package initializer for the healthcare ADK project.

Avoid importing `agent` here to prevent side effects during test collection
or when importing the package. Import `agent` explicitly where needed.
"""